# Token API 用户和学校管理实现总结

## ✅ 已完成的功能

### 1. 后端实现

#### 用户管理 API
- ✅ `backend/app/api/token_management.py` - 用户和学校管理端点

**用户管理端点**：
- `POST /api/v1/users` - 创建用户
- `GET /api/v1/users` - 列出用户（支持过滤）
- `GET /api/v1/users/{user_id}` - 获取用户详情
- `PUT /api/v1/users/{user_id}` - 更新用户信息
- `POST /api/v1/users/{user_id}/password` - 设置用户密码
- `DELETE /api/v1/users/{user_id}` - 删除用户

**学校管理端点**：
- `POST /api/v1/schools` - 创建学校
- `GET /api/v1/schools` - 列出所有学校
- `GET /api/v1/schools/{school_id}` - 获取学校详情
- `PUT /api/v1/schools/{school_id}` - 更新学校信息
- `DELETE /api/v1/schools/{school_id}` - 删除学校

### 2. 权限系统

#### Token 权限验证
- ✅ `read_users` - 可以查看用户和学校信息
- ✅ `manage_users` - 可以创建、编辑、删除用户，设置密码
- ✅ `manage_training` - 可以管理学校信息

#### 用户角色权限矩阵

| 操作 | Student | Teacher | School Admin | System Admin |
|------|---------|----------|--------------|---------------|
| 查看自己 | ✅ | ✅ | ✅ | ✅ |
| 创建学生 | ❌ | ✅ | ✅ | ✅ |
| 创建教师 | ❌ | ❌ | ✅ | ✅ |
| 创建管理员 | ❌ | ❌ | ❌ | ✅ |
| 编辑自己 | ✅ | ✅ | ✅ | ✅ |
| 编辑本校学生 | ❌ | ✅ | ✅ | ✅ |
| 删除学生 | ❌ | ✅ | ✅ | ✅ |
| 设置密码（自己） | ✅ | ✅ | ✅ | ✅ |
| 设置密码（学生） | ❌ | ✅ | ✅ | ✅ |
| 删除自己 | ❌ | ❌ | ❌ | ❌ |
| 管理学校 | ❌ | ❌ | ❌ | ✅ |

#### 权限验证规则

**创建用户**：
- 学生：不能创建用户
- 教师：只能创建本校学生
- 学校管理员：只能创建本校学生和教师
- 系统管理员：可以创建任意角色用户

**编辑用户**：
- 学生：只能编辑自己的信息（不能修改角色和学校）
- 教师：可以编辑本校学生
- 学校管理员：可以编辑本校任何用户
- 系统管理员：可以编辑任何用户

**设置密码**：
- 学生：只能设置自己的密码
- 教师：可以设置本校学生密码
- 学校管理员：可以设置本校任何用户密码
- 系统管理员：可以设置任何用户密码

**删除用户**：
- 不能删除自己的账户（所有角色）
- 学生：不能删除用户
- 教师：只能删除本校学生
- 学校管理员：只能删除本校学生和教师
- 系统管理员：可以删除任何用户

**管理学校**：
- 学生/教师/学校管理员：不能管理学校
- 系统管理员：可以创建、编辑、删除学校

### 3. 安全特性

- ✅ 完整的权限验证（基于 Token 权限和用户角色）
- ✅ 防止删除自己
- ✅ 防止跨学校操作（非管理员）
- ✅ 防止角色提升（如教师创建管理员）
- ✅ 学校删除前检查是否有用户
- ✅ 密码哈希存储（使用 Argon2）

### 4. 数据模型

**用户模型** (`CreateUserRequest`、`UpdateUserRequest`、`UserResponse`):
- username: 用户名
- password: 密码
- nickname: 昵称/姓名
- role: 角色 (student/teacher/school_admin/system_admin)
- school_id: 学校 ID
- created_at: 创建时间

**学校模型** (`CreateSchoolRequest`、`UpdateSchoolRequest`、`SchoolResponse`):
- name: 学校名称
- created_at: 创建时间
- user_count: 用户数量（动态计算）

### 5. 错误处理

| 状态码 | 场景 |
|--------|------|
| 400 Bad Request | 用户名已存在、角色无效、尝试删除自己、学校有用户 |
| 401 Unauthorized | Token 无效或已过期 |
| 403 Forbidden | 权限不足（基于 Token 和用户角色） |
| 404 Not Found | 用户/学校不存在 |

---

## 📋 API 端点完整列表

### Token 基础 API

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/v1/tokens/create` | POST | 创建外部访问令牌 |
| `/api/v1/tokens/verify` | POST | 验证令牌 |
| `/api/v1/tokens/me` | GET | 获取当前用户 |
| `/api/v1/tokens/revoke` | POST | 撤销令牌 |
| `/api/v1/tokens/config` | GET | 获取 API 配置 |
| `/api/v1/tokens/info` | GET | 获取 API 信息（公开） |

### Token 用户管理 API

| 端点 | 方法 | 说明 | 权限 |
|------|------|------|------|
| `/api/v1/users` | POST | 创建用户 | manage_users |
| `/api/v1/users` | GET | 列出用户 | read_users |
| `/api/v1/users/{user_id}` | GET | 获取用户详情 | read_users |
| `/api/v1/users/{user_id}` | PUT | 更新用户信息 | manage_users |
| `/api/v1/users/{user_id}/password` | POST | 设置用户密码 | manage_users |
| `/api/v1/users/{user_id}` | DELETE | 删除用户 | manage_users |

### Token 学校管理 API

| 端点 | 方法 | 说明 | 权限 |
|------|------|------|------|
| `/api/v1/schools` | POST | 创建学校 | manage_training |
| `/api/v1/schools` | GET | 列出所有学校 | read_users |
| `/api/v1/schools/{school_id}` | GET | 获取学校详情 | read_users |
| `/api/v1/schools/{school_id}` | PUT | 更新学校信息 | manage_training |
| `/api/v1/schools/{school_id}` | DELETE | 删除学校 | manage_training |

---

## 🔐 权限系统详解

### Token 权限到作用域映射

| Token 权限 | 最低作用域 | 说明 |
|-----------|-----------|------|
| `read_users` | read | 可以查看用户和学校信息 |
| `manage_users` | write | 可以创建、编辑、删除用户，设置密码 |
| `manage_training` | admin | 可以管理学校信息 |

### 用户角色到操作权限映射

```
┌──────────────────┬──────┬──────┬──────┬──────┬──────┬──────┬──────┬──────┐
│ 操作             │ Stud │ Teach │ S.   │ Sys  │      │      │      │      │
│                  │ ent  │ er   │ Admin│ Admin│      │      │      │      │
├──────────────────┼──────┼──────┼──────┼──────┼──────┼──────┼──────┼──────┤
│ 查看自己         │ ✅   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 查看本校学生     │ ❌   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 查看所有用户     │ ❌   │ ❌   │ ❌   │ ✅   │      │      │      │      │
│ 创建学生         │ ❌   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 创建教师         │ ❌   │ ❌   │ ✅   │ ✅   │      │      │      │      │
│ 创建管理员       │ ❌   │ ❌   │ ❌   │ ✅   │      │      │      │      │
│ 编辑自己         │ ✅   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 编辑本校学生     │ ❌   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 删除学生         │ ❌   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 设置密码（自己） │ ✅   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 设置密码（学生） │ ❌   │ ✅   │ ✅   │ ✅   │      │      │      │      │
│ 删除自己         │ ❌   │ ❌   │ ❌   │ ❌   │      │      │      │      │
│ 管理学校         │ ❌   │ ❌   │ ❌   │ ✅   │      │      │      │      │
└──────────────────┴──────┴──────┴──────┴──────┴──────┴──────┴──────┴──────┘
```

---

## 📝 代码文件

### 新增文件

- `backend/app/api/token_management.py` - 用户和学校管理 API

### 修改文件

- `backend/app/api/__init__.py` - 导入 token_management_router
- `backend/app/main.py` - 注册 token_management_router

### 文档

- `docs/TOKEN_USER_SCHOOL_MANAGEMENT.md` - 用户和学校管理完整文档

---

## 🧪 测试建议

### 1. 用户管理测试

```bash
# 获取 Token
TOKEN=$(curl -s -X POST http://localhost:8000/api/v1/tokens/create \
  -H "Content-Type: application/json" \
  -d '{
    "username": "teacher1",
    "password": "password123",
    "app_name": "TestApp",
    "scope": "write"
  }' | jq -r '.access_token')

# 测试：创建学生
curl -X POST http://localhost:8000/api/v1/users \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "test_student",
    "password": "password123",
    "nickname": "测试学生",
    "role": "student",
    "school_id": 1
  }'

# 测试：获取用户列表
curl -X GET "http://localhost:8000/api/v1/users?role=student&school_id=1" \
  -H "Authorization: Bearer $TOKEN"

# 测试：更新用户
curl -X PUT http://localhost:8000/api/v1/users/10 \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "nickname": "测试学生（更新）"
  }'

# 测试：设置密码
curl -X POST http://localhost:8000/api/v1/users/10/password \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "password": "newpassword456"
  }'

# 测试：删除用户
curl -X DELETE http://localhost:8000/api/v1/users/10 \
  -H "Authorization: Bearer $TOKEN"
```

### 2. 学校管理测试

```bash
# 需要 system_admin 角色
ADMIN_TOKEN=$(curl -s -X POST http://localhost:8000/api/v1/tokens/create \
  -H "Content-Type: application/json" \
  -d '{
    "username": "admin",
    "password": "admin_password",
    "app_name": "TestApp",
    "scope": "admin"
  }' | jq -r '.access_token')

# 测试：创建学校
curl -X POST http://localhost:8000/api/v1/schools \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试学校"
  }'

# 测试：列出学校
curl -X GET http://localhost:8000/api/v1/schools \
  -H "Authorization: Bearer $ADMIN_TOKEN"

# 测试：更新学校
curl -X PUT http://localhost:8000/api/v1/schools/5 \
  -H "Authorization: Bearer $ADMIN_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "测试学校（更新）"
  }'

# 测试：删除学校
curl -X DELETE http://localhost:8000/api/v1/schools/5 \
  -H "Authorization: Bearer $ADMIN_TOKEN"
```

---

## ✨ 总结

Token API 的用户和学校管理功能已完全实现，包括：

✅ **用户管理**：
- 创建用户（带角色和权限验证）
- 列出用户（支持过滤）
- 获取用户详情
- 更新用户信息
- 设置用户密码
- 删除用户（防止删除自己）

✅ **学校管理**：
- 创建学校（仅系统管理员）
- 列出所有学校
- 获取学校详情
- 更新学校信息
- 删除学校（检查是否有用户）

✅ **权限系统**：
- 基于 Token 权限的验证
- 基于用户角色的权限矩阵
- 完整的操作权限控制
- 跨学校访问限制

✅ **文档**：
- 完整的 API 文档
- 权限说明
- 使用示例（Python、JavaScript、cURL）
- 常见问题
- 最佳实践

所有功能已实现并可以正常使用！🎉
